# Release Please workflow - DISABLED
# This workflow created PRs for version bumps, but we prefer manual version bumps
# and then use the release.yml workflow when tags are pushed.
#
# If you want to re-enable automated PR-based releases, uncomment below:
#
# name: Release Please
#
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:
#
# permissions:
#   contents: write
#   pull-requests: write
#
# jobs:
#   release:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Release Please
#         uses: googleapis/release-please-action@v4
#         with:
#           config-file: .github/release-please-config.json
#           manifest-file: .release-please-manifest.json

